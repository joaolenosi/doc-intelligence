# Agentes

As instruções para agentes de IA neste repositório vivem em `CLAUDE.md`.

Este arquivo existe apenas para que agentes que procuram por `AGENTS.md` encontrem o caminho. O conteúdo não é duplicado de propósito, porque instrução duplicada é instrução que diverge.
